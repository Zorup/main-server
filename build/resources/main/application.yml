spring:
  datasource:
    url: jdbc:mariadb://localhost:18888/board
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: mariadb
  mvc:
    hiddenmethod:
      filter:
        enabled: true
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
   # hibernate:
     # ddl-auto: create

  jwt:
    secret: 2021!!ZorupMSA

  messages:
    basename: i18n/exception
    encoding: UTF-8

server:
  port: 8082